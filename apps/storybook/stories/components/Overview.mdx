import { Meta } from '@storybook/blocks';

<Meta title="Components/Overview" />

<div style={{ maxWidth: 900, fontFamily: 'system-ui, -apple-system, sans-serif' }}>

# Components

30 production-ready components across 6 categories. All components are token-driven, fully typed, and WCAG 2.1 AA compliant.

---

## Inputs

Form controls for capturing user input.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 8, margin: '16px 0 32px' }}>
  {[
    { name: 'Button',       desc: 'Trigger actions. 4 variants, 5 sizes, 6 colors.' },
    { name: 'TextField',    desc: 'Single and multi-line text input with labels and validation.' },
    { name: 'Select',       desc: 'Single-choice dropdown with search support.' },
    { name: 'Autocomplete', desc: 'Searchable select with free-text and multi-value modes.' },
  ].map(c => (
    <div key={c.name} style={{ background: '#fff', border: '1px solid #E5E7EB', borderRadius: 8, padding: '16px' }}>
      <div style={{ fontWeight: 700, fontSize: 14, color: '#111827', marginBottom: 4 }}>{c.name}</div>
      <p style={{ margin: 0, fontSize: 12, color: '#6B7280', lineHeight: 1.4 }}>{c.desc}</p>
      <div style={{ marginTop: 10 }}>
        <span style={{ fontSize: 11, fontWeight: 600, color: '#059669', background: '#D1FAE5', padding: '2px 6px', borderRadius: 10 }}>✅ Wave 1</span>
      </div>
    </div>
  ))}
</div>

## Selection

Controls for making choices.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 8, margin: '16px 0 32px' }}>
  {[
    { name: 'Checkbox', desc: 'Multi-select boolean control. Indeterminate state supported.' },
    { name: 'Radio',    desc: 'Single-select within a group. Uses RadioGroup for layout.' },
    { name: 'Switch',   desc: 'Toggle between on/off. Labeled with left or right placement.' },
    { name: 'Chip',     desc: 'Compact tag or filter. Filled and outlined variants.' },
  ].map(c => (
    <div key={c.name} style={{ background: '#fff', border: '1px solid #E5E7EB', borderRadius: 8, padding: '16px' }}>
      <div style={{ fontWeight: 700, fontSize: 14, color: '#111827', marginBottom: 4 }}>{c.name}</div>
      <p style={{ margin: 0, fontSize: 12, color: '#6B7280', lineHeight: 1.4 }}>{c.desc}</p>
      <div style={{ marginTop: 10 }}>
        <span style={{ fontSize: 11, fontWeight: 600, color: '#059669', background: '#D1FAE5', padding: '2px 6px', borderRadius: 10 }}>✅ Wave 1</span>
      </div>
    </div>
  ))}
</div>

## Feedback

Communicate status, progress, and system messages.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 8, margin: '16px 0 32px' }}>
  {[
    { name: 'Alert',           desc: '4 severity levels. Filled, outlined, and standard variants.', wave: 1 },
    { name: 'Snackbar',        desc: 'Transient notification. Severity embedding with live regions.', wave: '2B' },
    { name: 'CircularProgress',desc: 'Spinning arc. Determinate and indeterminate modes.', wave: '2A' },
    { name: 'LinearProgress',  desc: 'Horizontal bar. 7 color variants. Rounded option.', wave: '2A' },
    { name: 'Skeleton',        desc: 'Animated loading placeholder. Text, rectangle, circle.', wave: '2A' },
  ].map(c => (
    <div key={c.name} style={{ background: '#fff', border: '1px solid #E5E7EB', borderRadius: 8, padding: '16px' }}>
      <div style={{ fontWeight: 700, fontSize: 14, color: '#111827', marginBottom: 4 }}>{c.name}</div>
      <p style={{ margin: 0, fontSize: 12, color: '#6B7280', lineHeight: 1.4 }}>{c.desc}</p>
      <div style={{ marginTop: 10 }}>
        <span style={{ fontSize: 11, fontWeight: 600, color: '#059669', background: '#D1FAE5', padding: '2px 6px', borderRadius: 10 }}>✅ Wave {c.wave}</span>
      </div>
    </div>
  ))}
</div>

## Navigation

Help users move through the product.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 8, margin: '16px 0 32px' }}>
  {[
    { name: 'Tabs',        desc: 'Tab strip with keyboard nav. Compound Tabs.Panel for content regions.', wave: '2C' },
    { name: 'Breadcrumbs', desc: 'Hierarchical nav trail. Default chevron separator from icon system.', wave: '2C' },
    { name: 'Link',        desc: 'Inline typographic link. Primary, secondary, and inherit color variants.', wave: '2C' },
    { name: 'Pagination',  desc: 'Page navigation. Multiple variants (text, outlined, rounded).', wave: 1 },
    { name: 'Menu',        desc: 'Dropdown list. Keyboard navigation. Dense and icon variants.', wave: 1 },
  ].map(c => (
    <div key={c.name} style={{ background: '#fff', border: '1px solid #E5E7EB', borderRadius: 8, padding: '16px' }}>
      <div style={{ fontWeight: 700, fontSize: 14, color: '#111827', marginBottom: 4 }}>{c.name}</div>
      <p style={{ margin: 0, fontSize: 12, color: '#6B7280', lineHeight: 1.4 }}>{c.desc}</p>
      <div style={{ marginTop: 10 }}>
        <span style={{ fontSize: 11, fontWeight: 600, color: '#059669', background: '#D1FAE5', padding: '2px 6px', borderRadius: 10 }}>✅ Wave {c.wave}</span>
      </div>
    </div>
  ))}
</div>

## Surfaces

Structural containers and display elements.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 8, margin: '16px 0 32px' }}>
  {[
    { name: 'Card',    desc: 'Content container. Compound Header/Content/Footer. 3 variants.', wave: 1 },
    { name: 'List',    desc: 'Vertical list of items. Dense mode. Icon support. Selected state.', wave: 1 },
    { name: 'Divider', desc: 'Horizontal or vertical separator. Light, medium, dark weights.', wave: 1 },
    { name: 'Avatar',  desc: 'User image, initials, or icon. 5 sizes. Image/letter/icon variants.', wave: 1 },
    { name: 'Badge',   desc: 'Notification count or dot overlaid on content. 4 colors.', wave: 1 },
    { name: 'Tooltip', desc: 'Informational label on hover/focus. 12 placement options.', wave: 1 },
  ].map(c => (
    <div key={c.name} style={{ background: '#fff', border: '1px solid #E5E7EB', borderRadius: 8, padding: '16px' }}>
      <div style={{ fontWeight: 700, fontSize: 14, color: '#111827', marginBottom: 4 }}>{c.name}</div>
      <p style={{ margin: 0, fontSize: 12, color: '#6B7280', lineHeight: 1.4 }}>{c.desc}</p>
      <div style={{ marginTop: 10 }}>
        <span style={{ fontSize: 11, fontWeight: 600, color: '#059669', background: '#D1FAE5', padding: '2px 6px', borderRadius: 10 }}>✅ Wave {c.wave}</span>
      </div>
    </div>
  ))}
</div>

## Overlays

Components that render above page content in a portal.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(200px, 1fr))', gap: 8, margin: '16px 0 32px' }}>
  {[
    { name: 'Dialog',  desc: 'Modal with focus trap. Compound Title/Content/Actions. Loading state.', wave: '2B' },
    { name: 'Drawer',  desc: 'Sliding panel on any edge. Compound Header/Body. 3 variants.', wave: '2D' },
    { name: 'Popover', desc: 'Anchored surface. No focus trap. Compound Content wrapper.', wave: '2D' },
    { name: 'Backdrop',desc: 'Full-viewport scrim. Loading states and custom blocking UIs.', wave: '2A' },
  ].map(c => (
    <div key={c.name} style={{ background: '#fff', border: '1px solid #E5E7EB', borderRadius: 8, padding: '16px' }}>
      <div style={{ fontWeight: 700, fontSize: 14, color: '#111827', marginBottom: 4 }}>{c.name}</div>
      <p style={{ margin: 0, fontSize: 12, color: '#6B7280', lineHeight: 1.4 }}>{c.desc}</p>
      <div style={{ marginTop: 10 }}>
        <span style={{ fontSize: 11, fontWeight: 600, color: '#059669', background: '#D1FAE5', padding: '2px 6px', borderRadius: 10 }}>✅ Wave {c.wave}</span>
      </div>
    </div>
  ))}
</div>

</div>
